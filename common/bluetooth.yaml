---
esp32_ble_tracker:
  scan_parameters:
    interval: 1100ms # default 320ms
    window: 1100ms # default 30ms
    active: true

# esp32_improv:
#   authorizer: none

# bluetooth_proxy:
#   active: true
